<view class="warp">
    <map id="map" 
        markers="{{markers}}" 
        latitude="{{latitude}}" 
        longitude="{{longitude}}"
        bindregionchange="mapMoveHandler"
        >
    
    </map>
</view>
